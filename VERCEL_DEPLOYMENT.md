# خطوات تحديث الموقع على Vercel

## الطريقة الأولى: التحديث التلقائي (موصى به)

إذا كان الموقع مربوطاً بـ GitHub، Vercel سيحدث الموقع تلقائياً عند رفع التحديثات:

1. ✅ **تم رفع التحديثات إلى GitHub** (تم الآن)
2. انتقل إلى [Vercel Dashboard](https://vercel.com/dashboard)
3. Vercel سيكتشف التحديثات تلقائياً ويبدأ البناء
4. يمكنك متابعة عملية البناء في قسم "Deployments"
5. بعد اكتمال البناء، سيتم النشر تلقائياً

---

## الطريقة الثانية: النشر اليدوي

إذا كان التحديث التلقائي غير مفعّل:

### عبر Vercel CLI:

```bash
# تثبيت Vercel CLI (إذا لم يكن مثبتاً)
npm install -g vercel

# الانتقال إلى مجلد المشروع
cd "d:\08 Tech Group\platform"

# تسجيل الدخول إلى Vercel
vercel login

# النشر
vercel --prod
```

### عبر Vercel Dashboard:

1. انتقل إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر المشروع
3. اضغط على "Deployments"
4. اضغط على "..." بجانب آخر deployment
5. اختر "Redeploy"
6. أو اضغط على "Deploy" → "Deploy with Git" لإعادة البناء من آخر commit

---

## إعدادات Vercel المطلوبة

تأكد من أن الإعدادات التالية موجودة في Vercel Dashboard:

### Build Settings:
- **Framework Preset:** Next.js
- **Build Command:** `npm run build`
- **Output Directory:** `out` (لأن المشروع يستخدم static export)
- **Install Command:** `npm install`

### Environment Variables:
- لا توجد متغيرات بيئة مطلوبة (المشروع static بالكامل)

### Root Directory:
- اتركه فارغاً (المشروع في الجذر)

---

## التحقق من النشر

بعد اكتمال النشر:

1. ✅ افتح الموقع في المتصفح
2. ✅ تحقق من أن الموقع يعمل بشكل صحيح
3. ✅ تحقق من القائمة السفلية على الجوال
4. ✅ تحقق من أن جميع الصفحات تعمل
5. ✅ تحقق من أن PWA يعمل (يمكن تثبيت الموقع على الجوال)

---

## ملاحظات مهمة

- ⚠️ **Static Export:** المشروع يستخدم `output: 'export'`، لذا Vercel سيبني static files
- ⚠️ **Output Directory:** الملفات في مجلد `out` (سيتم نسخها إلى `dist` عبر script)
- ⚠️ **Build Command:** يجب أن يكون `npm run build` (يحتوي على copy-to-dist)
- ✅ **Clean URLs:** المشروع يدعم clean URLs مع trailing slash

---

## استكشاف الأخطاء

إذا واجهت مشاكل:

1. **تحقق من Build Logs:**
   - اذهب إلى Vercel Dashboard → المشروع → Deployments → آخر deployment
   - افتح "Build Logs" للتحقق من الأخطاء

2. **تحقق من Output Directory:**
   - تأكد أن `outputDirectory` في Vercel هو `out`

3. **تحقق من Build Command:**
   - يجب أن يكون: `npm run build`

4. **تحقق من Node Version:**
   - Vercel يستخدم Node.js 18.x أو 20.x بشكل افتراضي
   - تأكد من أن المشروع متوافق

---

## الاتصال بـ GitHub (إذا لم يكن مربوطاً)

إذا أردت ربط المشروع بـ GitHub للتحديث التلقائي:

1. اذهب إلى Vercel Dashboard
2. اضغط على "Add New..." → "Project"
3. اختر GitHub repository
4. Vercel سيكتشف الإعدادات تلقائياً
5. تأكد من:
   - Framework: Next.js
   - Root Directory: ./
   - Build Command: `npm run build`
   - Output Directory: `out`
6. اضغط "Deploy"

بعد ذلك، كل push إلى GitHub سيحدث الموقع تلقائياً! 🚀
